---
---
<header class="border-b border-slate-900 bg-black/80 backdrop-blur-xl sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between">
    <a href="/" class="text-xl font-black tracking-tighter text-white">
      TRADE<span class="text-emerald-500">90</span>
    </a>

    <nav class="hidden md:flex items-center gap-8 text-[10px] font-black uppercase tracking-[0.2em]">
      <a href="/" class="text-slate-400 hover:text-white transition-colors">Calculator</a>
      <a href="/blog" class="text-slate-400 hover:text-white transition-colors">Analysis</a>
      <a href="#portal/signin" class="text-slate-400 hover:text-white transition-colors">Login</a>
      <a href="#portal/signup" class="bg-emerald-500 text-black px-5 py-2 rounded-full hover:bg-emerald-400 transition-all">Join Now</a>
    </nav>

    <button id="menu-toggle" class="md:hidden text-white p-2" aria-label="Toggle Menu">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-menu"><line x1="3" x2="21" y1="12" y2="12"></line><line x1="3" x2="21" y1="6" y2="6"></line><line x1="3" x2="21" y1="18" y2="18"></line></svg>
    </button>
  </div>

  <div id="mobile-menu" class="hidden md:hidden bg-slate-950 border-b border-slate-900 absolute w-full left-0 p-6 space-y-6 shadow-2xl">
    <nav class="flex flex-col gap-6 text-sm font-bold uppercase tracking-widest">
      <a href="/" class="text-slate-300">Calculator</a>
      <a href="/blog" class="text-slate-300">Analysis</a>
      <hr class="border-slate-900" />
      <a href="#portal/signin" class="text-emerald-500">Login</a>
      <a href="#portal/signup" class="bg-emerald-500 text-black text-center py-4 rounded-xl font-black">Join Now</a>
    </nav>
  </div>
</header>

<script>
  // Mobile Menu Logic
  const btn = document.getElementById('menu-toggle');
  const menu = document.getElementById('mobile-menu');

  btn?.addEventListener('click', () => {
    menu?.classList.toggle('hidden');
  });

  // Close menu when a link is clicked
  const links = menu?.querySelectorAll('a');
  links?.forEach(link => {
    link.addEventListener('click', () => {
      menu?.classList.add('hidden');
    });
  });
</script>
